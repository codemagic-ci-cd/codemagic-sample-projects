import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codemagic.ioniccapacitor',
  appName: 'IonicCapacitor',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
